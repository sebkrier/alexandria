# Production Environment Variables for Railway
# These should be set in Railway's dashboard, not committed to git

# Database - Railway provides this automatically when you add PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:password@host:5432/railway

# Security - GENERATE THESE FRESH FOR PRODUCTION
# Run: openssl rand -hex 32
ENCRYPTION_KEY=your-production-encryption-key-change-this

# Cloudflare R2 Storage
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=alexandria-files
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# App Settings
DEBUG=false
CORS_ORIGINS=["https://your-frontend-url.up.railway.app"]
