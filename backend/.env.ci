# ===========================================
# Alexandria CI Environment Configuration
# ===========================================
# Used by GitHub Actions for automated testing.
# NOT for production use - contains placeholder values.

# Database Connection (matches GitHub Actions service container)
DATABASE_URL=postgresql+asyncpg://postgres:testpass@localhost:5432/alexandria_test
TEST_DATABASE_URL=postgresql+asyncpg://postgres:testpass@localhost:5432/alexandria_test

# Security Keys (test-only, not for production!)
ENCRYPTION_KEY=ci-test-encryption-key-not-for-production-use

# Application Settings
DEBUG=false
CORS_ORIGINS=["http://localhost:3000"]

# Cloudflare R2 Storage (not needed for tests)
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=

# AI Provider API Keys (not needed - tests mock AI calls)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
